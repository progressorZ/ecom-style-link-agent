# 拼多多单款填表助手使用说明

更新：2026-09-12。本机单人使用，一次处理一款女装 T 恤。普通用户操作表单，不需要查看或编辑 JSON。工具自动填写已支持内容、回读核对；平台额外必填项和最终发布由商家处理。

## 先了解它的作用

这个助手用于减少拼多多发布女装 T 恤时重复填写表单的时间。你在本地工作台录入一次标题、商品属性、颜色尺码、价格库存、尺寸表和图片，助手会把已支持内容填入拼多多商品编辑页，再把填写结果读回来供你核对。

```text
准备商品资料 → 工作台录入或复用模板 → 登录拼多多 → 自动填写并核对 → 手动发布
```

第一次使用只需记住五个动作：

1. 解压并启动助手。
2. 在工作台填写真实商品资料；不会填时先载入完整示例。
3. 点击“检查资料并载入”。
4. 打开商家浏览器、登录并进入女装 T 恤的新商品编辑页。
5. 点击“填写并核对”，检查平台页面后手动发布。

## 1. 启动与更新

### Windows 免安装便携版

将 `pdd-womenswear-tshirt-v0.1.4-windows-x64.zip` 完整解压后，双击 `Start-PDD-Assistant.cmd`。完整包内置 Node.js，朋友无需安装开发环境，也不需要首次联网下载运行环境；登录拼多多仍需联网。电脑需要 Windows 10/11 64 位，并能正常使用 Microsoft Edge 或 Google Chrome。不要在 ZIP 压缩包预览窗口内直接运行。

个人商品资料、图片副本、报告和专用浏览器登录状态保存在便携包的 `data` 文件夹。更新时只迁移自己的 `data` 文件夹；使用后的 `data` 不要分享给别人。启动异常时双击 `Diagnose-PDD-Assistant.cmd`。

开发者生成新版便携包：

```sh
npm run build:windows-portable
```

成品及 SHA-256 校验文件生成在 `release` 目录。打包过程不会复制本机现有的 `.runtime`、`output` 或店铺配置。

Mac 双击项目中的“启动工作台.command”，或在项目目录执行：

```sh
npm run workbench:start
```

访问 http://127.0.0.1:5173/ 。首次安装需要 Node.js 24.15 以上、Google Chrome，并运行 `npm install`。

更新后台代码后，先保存当前操作，再执行以下命令并刷新网页。重启可能关闭商家浏览器，需要重新登录。

```sh
npm run workbench:stop
npm run workbench:start
```

“本机服务已连接”不等于已登录拼多多。故障诊断用 `npm run workbench:doctor`，不要靠删除数据或反复重启解决验证码。

## 第一次使用：完整初始示例

在录入区展开“首次使用：完整初始版与操作指引”，点击“载入完整初始示例”。空白商品会默认展开引导，已有资料不会自动覆盖。

初始版包含全部当前必填属性、白色/黑色、S/M/L共6个规格、价格库存、主图详情图及颜色图、肩宽胸围腰围臀围，以及可切换查看的身高体重区间。图片是标有测试说明的占位图，数值仅演示格式，不能视为真实商品数据。

按引导逐项修改自己的店铺运费、标题属性、颜色尺码、价格库存、实测尺寸和图片。引导按钮可定位到对应区域，示例不会替你填虚构店铺，也不会自动打开商家页或发布。数据检查通过后仍需人工核实确认。

载入示例会替换当前表单，但可以点击“撤销上次套用 / 替换”恢复。示例来源提示随商品复制、模板保存保留。修改成自己的资料后可另存商品模板，下次直接选择复用。

## 2. 配置一次店铺

展开“首次设置 / 修改店铺与运费”，填写真实店铺名、店铺编号、现有运费模板名称及地区收费规则。核实后保存。店铺编号不是商品 ID。

当前支持已有的“其他运费模板”，不创建平台运费规则。店铺与运费是全局配置，所有商品和模板使用当前配置；平台执行时仍会核对。

## 3. 新建、复制和继续编辑

- **新建空白商品**：自动生成可修改的商品货号，如 `A20260912-001`。有供应商货号可替换。
- **复制上一款全部资料**：复制最近一次已检查并载入的商品，生成新货号和规格编码。未载入的修改请先保存为商品模板。
- **继续编辑当前商品**：恢复最近一次载入资料，保留原货号。
- **只复用品牌与属性**：只覆盖这两部分，其他资料保留。
- **撤销上次套用 / 替换**：恢复最近一次整表替换前的资料，包括商品模板、尺码模板、新建、复制和继续编辑。只支持一次，在本次网页会话内有效，刷新后不可撤销；撤销后须重新确认载入。

复制或套用后，价格库存、图片、颜色尺码及尺码表必须重新确认。修改对应资料会撤销相应确认，刷新后仍保留复用来源；平台商品 ID、草稿关联和上传回执不会复制。

## 4. 多个商品模板

1. 修改当前表单，填写“模板标题”，点击“另存为新模板”。允许保存未完成资料，不必先载入。
2. 从列表选择模板，点击“套用为新商品”。会替换当前表单、生成新货号和规格编码，必要时可撤销。
3. 修改后可另存新模板，或“用当前资料更新所选模板”。模板标题独立于商品标题，更新一个模板不影响其他模板。
4. 点击“删除所选商品模板”，核对弹出的模板名称后确认删除。当前商品和图片不删除。

模板保存商品属性、颜色尺码、价格库存、图片、发货服务和完整尺码表。模板标题不可重复。规格不完整或重复时仍可套用，修正后才能载入平台。

## 5. 颜色、尺码、价格与库存

颜色和尺码用逗号分隔，例如颜色 `白色,黑色`，尺码 `S,M,L`。离开输入框后自动更新规格表，也可点击“生成 / 更新颜色尺码规格”。最多10种颜色、40个组合，每种颜色使用相同尺码集合。

SKU 是 **Stock Keeping Unit**（库存管理单位），服装中指具体的“颜色＋尺码”组合。例如白色/S、白色/M、黑色/S、黑色/M 是4个规格，属于同一款商品。编码由系统管理，无需手填。

逐行填写拼单价、单买价和库存。金额单位元，最多两位小数；库存是明确的非负整数，空白不会当作0。停用规格库存必须为0，至少保留一个启用规格。新增组合的价格库存留空，不自动编造。

当前模板要求单买价不低于拼单价、参考价高于所有单买价，并设置满2件5.0～9.9折。不适用此促销的商品需人工在平台处理。

## 6. 实测尺码与身高体重建议

选择一种表型，每件商品当前向平台填写其中一种：

- **成衣实测尺寸**：肩宽、胸围、腰围、臀围接入自动填写，单位cm；衣长、袖长可记录和复用，但须在平台或详情图人工补充。
- **身高体重选码建议**：身高cm、体重kg（不是斤），按供应商资料或已确认的试穿结果填写，系统不推算建议值。

每列可以勾选“区间值”，分别填上下限。一列的所有尺码模式一致；需正数、不超过300、最多一位小数，下限不能大于上限。两种表型分别记住字段和区间设置，来回切换不重置。

独立尺码模板支持命名另存、选择套用、更新和删除。按尺码名称匹配，未匹配行留空，不改变销售颜色尺码或价格库存。套用会替换当前尺码表数据，可用撤销恢复。修改名称后“更新所选尺码模板”可以重命名；另存使用不同名称。

平台整张尺码表为空时，可先添加所需列，再移除多余列，并切换区间模式。只要有任何已有尺寸（包括一个区间端点）就不自动改列或模式；保留原页供人工处理。平台缺少对应控件时也会停止，不猜测操作。

## 7. 图片

- 主图、详情图各最多10张；每种颜色选择一张规格图，最多10种颜色。
- JPG/PNG，单张不超过3MB，宽高均超过480像素，比例1:1或3:4。这是当前执行器限制，长详情图暂不支持。
- 主图第一张是封面，可上移、移除。颜色规格图显示缩略图和文件名，可替换或移除。
- 主图或详情图同一组内不允许重复内容；不同颜色的规格图允许明确选择同一文件，是否符合实物由用户核实。
- **载入测试图片（仅测试草稿）**：使用随项目保存的占位图，支持多个颜色；不需要进入隐藏目录。测试图片不能当作真实商品图片发布。

图片数量在选择和载入前检查。模板引用本机保存的图片；图片被删除后需重新选择，不会凭文件名恢复。

## 8. 填写拼多多

1. 核实资料、图片、店铺和服务设置，勾选确认，点击“检查资料并载入”。这一步不发布商品。
2. 打开商家浏览器并登录，选择“女装/女士精品 → T恤 → T恤”，进入新商品编辑页。
3. 在工作台选择目标编辑页，点击“填写并核对”。新款使用新的空白编辑页，不重复覆盖上一款。
4. 看回读结果及待人工处理项。图片内容仍需人工核实；额外资质或必填项在平台补齐。
5. 价格库存或图片差异先在原页人工修正，再点“核对商品”。不要为局部差异反复全量填表、上传。
6. 标题及已支持属性可使用局部修正；保存草稿、重开草稿是辅助功能。
7. 商家最后在拼多多手动发布。草稿不等于上架，审核未完成不等于可售链接。

验证码由人工完成，正常任务复用登录浏览器。工作台不会自动发布。

## 9. 数据与排查

| 内容 | 保存位置 |
|---|---|
| 未载入表单 | 当前浏览器本地存储 |
| 商品、配置、任务记录、图片 | `output/live-workbench/` |
| 商品模板 | `output/live-workbench/product-templates.json` |
| 尺码模板 | `output/live-workbench/size-templates.json` |
| 最近一次删除前模板快照 | 对应模板文件后缀 `.json.backup` |

删除模板不删除图片。备份快照仅保留最近一次删除前的列表，不是完整版本历史；误删时先停止继续删除，再按备份恢复。不要清空浏览器数据或删除工作台目录来处理普通故障。

JSON 导入只供开发者排查；普通用户不需要逐项确认JSON。错误时保留当前页面，可下载任务报告辅助定位。

## 10. 验收与后续

最新改动通过本地检查不代表真实拼多多页面全部验收。真实验收由用户执行，重点覆盖单色、多色、不同价格库存、身高体重区间、空表换列和模板隔离，并记录实际耗时。

批量、自动发布、AI识图/文案/图片/视频、更多类目、多平台和SaaS尚未接入，不计入当前单款MVP完成条件。后续事项见 `docs/53-next-steps.md`。


## 11. 从现有表格直接粘贴

价格库存区域展开“从表格粘贴价格库存”。复制Excel/WPS中的表头及当前商品数据，也可粘贴逗号分隔文本：

```text
颜色,尺码,拼单价,单买价,库存
白色,S,99,109,20
白色,M,99,109,30
```

先录入当前颜色尺码并生成规格表。粘贴按颜色＋尺码精确匹配，不按行号覆盖；可以只提供库存等部分列，也可只更新部分规格。没有提供的列保持原值，提供了但留空会报错。未知颜色尺码、重复行、未知表头及无效价格库存均拒绝整次应用，不做部分写入。

尺码区域展开“从表格粘贴尺码尺寸”，先选择成衣或身高体重表型，复制当前所有尺码。例如身高体重：

```text
尺码,身高下限(cm),身高上限(cm),体重(kg)
S,155,160,40-50
M,160,165,50-60
```

成衣列支持肩宽、胸围、腰围、臀围、衣长、袖长；衣长袖长仍按前述范围人工补充。可用一列区间如40-50，也可用完整的上下限两列。一列所有行的模式必须一致，不猜测缺失端点、不把斤自动换成kg。尺码必须覆盖当前商品所有尺码，未提供的其他尺寸字段保持原值。

两种粘贴均先点击“预览粘贴结果”，核对原值和新值，再点击“确认应用粘贴数据”。预览后若表单发生变化，需要重新预览。应用会撤销资料确认，可以用页面顶部撤销恢复。

## 12. 属性候选和集中检查

属性输入框可筛选已有历史实店候选；候选来源为项目留存的T恤页面采集/回读记录，不是实时或完整的平台枚举。没有可靠候选的条件仍可手填平台原文。

修改面料俗称会清空材质及成分选项，修改主风格会清空下级风格，需重新选择；系统不会自动推算面料成分。已知联动条件下显示对应候选，未知条件不借用别的条件选项。

点击“检查全部资料（无需先勾选确认）”，一次列出标题属性、规格价格库存、图片选择、尺码数值等本地输入问题。点击问题定位到对应区域，修改后列表更新。该检查不替代文件实际存在性、平台当前选项、实时页面及特殊资质核对；后端载入还会执行这些已实现的后续校验。
