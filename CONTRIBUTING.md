# 贡献指南

感谢你帮助沉淀电商刊登中的重复工作。本项目欢迎平台 Adapter、类目配置、导入器、素材工具、测试证据、文档和用户体验改进。

## 开始之前

1. 较大的新方向先在 GitHub Discussions 的 Ideas 分类讨论；范围明确后，在 Issue 中说明平台、站点、类目、商家后台入口、准备实现的能力和验证条件。
2. 不要提交账号、Cookie、二维码、手机号、订单、店铺密钥、真实用户图片或带身份信息的页面快照。
3. 新平台事实必须来自贡献者有权访问的页面或官方文档，并记录验证日期、站点、叶子类目和适用范围。
4. 未经证据支持的字段名、限制和平台行为标为 `unverified`，不能写成全平台规则。

使用问题进入 Discussions 的 Q&A 分类，不创建 Bug Issue。完整分流规则见 [COMMUNITY.md](COMMUNITY.md)。

## 推荐贡献单位

一个 Pull Request 尽量只完成一个可验收能力，例如“淘宝女装 T 恤标题填写与回读”或“Shopee 颜色尺码矩阵编译”。不要在同一个改动中同时引入平台、AI 服务、批量队列和发布动作。

## App 与 Adapter

Adapter 是开发者维护的平台/类目执行能力；App 是给普通用户下载的具体表单工具。一个 App 在 `apps/<app-id>/app.json` 中引用一个 Adapter 和明确类目，拥有独立版本、数据命名空间、Windows 包及 macOS 包。不要制作一个要求用户选择所有平台的总客户端。

## Adapter 最小结构

```text
adapters/<adapter-id>/
├── adapter.json       # 身份、兼容性、能力和成熟度
├── README.md          # 用户范围、配置、已知限制
├── fixtures/          # 脱敏 DOM/输入/回读样例
└── evidence/          # 只放脱敏摘要或指向 docs 的说明
```

当前运行代码仍位于 `scripts/`。迁移期允许 `adapter.json` 的 entrypoints 指向现有文件；新平台不要继续增加 `pdd-*` 式全局文件，应先建立自己的 Adapter 目录。详细字段见 [Adapter 贡献规范](docs/55-adapter-contribution-spec.md)。

类目资料字段优先登记到 `config/category-profiles.json`，并通过 `schemas/category-profile.schema.json` 校验。Profile 描述商品事实；平台页面原文、选择器、枚举 ID 和控件依赖属于 Adapter 页面合同。没有真实证据时将字段标记为 `unverified`，且编译结果必须保持不可执行。

## 代码边界

- `ProductPackage` 保存跨平台商品事实和刊登意图，平台字段 ID、DOM 选择器和平台枚举不得写入商品核心。
- Adapter 负责将核心数据编译为平台计划，并执行 `preflight → fill → readback`。写入后必须回读，不允许只凭点击成功判断完成。
- 浏览器操作优先采用确定性定位。AI 可以提出候选或解释异常，但不能自行决定价格、库存、尺寸、店铺身份或最终发布。
- 新增发布动作必须单独设计权限、幂等、结果核对和人工门禁。当前项目不接受默认自动发布。
- 日志记录业务键和步骤结果，不记录会话凭证与完整页面源码。

## 验证要求

成熟度从低到高需要以下证据：

1. `proposal`：问题说明、字段清单和预期边界。
2. `experimental`：Schema 校验、编译测试、DOM 夹具测试、错误分支和无发布证明。
3. `beta`：真实测试店铺的空白页预检、受控填写、逐字段回读、保存草稿并重开核对；证据必须脱敏。
4. `stable`：至少两轮不同日期或页面版本的验证、明确维护者、兼容性记录和可操作的降级说明。

每次提交至少运行与修改有关的测试；当前参考实现通常运行：

```sh
npm test
npm run test:mvp
npm run test:adapter-manifests
npm run build
```

真实页面验证由有权限的商家在测试商品上完成。测试不得点击最终发布，除非项目未来建立了单独、明确授权的发布测试流程。

## Pull Request 应说明

- 平台、站点、类目和触发页面。
- 用户之前需要手工完成什么，改动后由系统完成什么。
- 支持及不支持的字段和状态。
- 模拟测试与真实页面证据分别有哪些。
- 页面变化时如何停止、提示和恢复。
- 是否涉及写入、保存草稿或发布；默认写明“不会自动发布”。

## 许可证

本项目采用 Apache License 2.0。除非贡献者明确书面声明某次提交不是贡献，提交并被接收的代码与文档按项目许可证提供。新增第三方代码或素材时必须登记其来源与兼容许可证。
