# 隐私说明

本项目采用本地优先设计。工作台和本机后台默认只监听 `127.0.0.1`，项目维护者不运营用于接收商品资料、图片或账号凭据的云服务，也没有内置遥测、广告或用户行为上报。

## 本机保存的内容

使用具体 App 时，电脑上可能保存以下数据：

- 商品标题、货号、品牌、属性、颜色尺码、价格、库存、尺码表和服务设置。
- 用户选择的商品图片副本、商品模板、导入结果、任务报告、差异报告和只读选项采集报告。
- 店铺名称、店铺编号、已有运费模板名称及用户填写的规则摘要。
- 专用商家浏览器的配置和登录状态，其中可能包含 Cookie、站点存储及其他会话信息。

便携包统一把资料放在解压目录的 `data` 下，并按 App 使用独立子目录。例如女装业务资料位于 `data/output/live-workbench`、浏览器资料位于 `data/.runtime/pdd-workbench-profile`；板鞋业务资料位于 `data/output/board-shoes-workbench`、浏览器资料位于 `data/.runtime/pdd-board-shoes-profile`。源码开发环境可以通过 `ECOM_DATA_DIR` 改变数据根目录；未设置时使用项目工作目录下的对应路径。浏览器页面中的当前表单和部分模板也会保存在该浏览器的本机存储中。

## 网络访问

工作台页面与本机后台之间只通过本机地址通信。用户点击“打开商家浏览器”后，专用浏览器会直接访问拼多多等目标平台；这些访问受相应平台的隐私政策和账号规则约束。

安装依赖、下载发行包或检查更新时使用的 npm、GitHub 等服务有各自的隐私政策。项目本身不会把工作台业务资料发送给项目维护者。第三方贡献的 Adapter 是可执行代码，使用前应检查其来源、网络访问和权限。

## 备份与导出

“一键下载备份”包含业务资料、图片副本、设置、模板和任务报告，但不包含专用商家浏览器的登录状态。导出的商品草稿、采集报告和任务报告仍可能包含商品原文、平台页面地址或本机文件路径，应按敏感业务资料保管。

备份文件不会自动上传。保存到网盘、聊天工具、移动硬盘或其他电脑后，相应副本由用户自行管理。不要把使用后的 `data` 目录、备份文件或未脱敏报告上传到公开 Issue、Discussion、Pull Request 或代码仓库。

## 删除本机数据

1. 先关闭助手启动窗口和专用商家浏览器。
2. 便携包用户可删除解压目录中的 `data` 文件夹；这会删除业务资料和专用浏览器登录状态。
3. 源码用户可删除 `ECOM_DATA_DIR` 指向的数据目录；使用默认开发目录时，检查 `output/live-workbench` 与 `.runtime/pdd-workbench-profile`。
4. 清除工作台所用浏览器对 `127.0.0.1:5173` 的站点数据，可删除保存在浏览器本机存储中的当前表单和模板。
5. 单独保存到其他位置的备份和导出文件需要分别删除。

删除数据前请确认是否仍需模板、图片或任务报告。项目没有远程账户，也无法替用户恢复已删除的本机资料。

## 分享设备和提交问题

在共享电脑上使用时，应使用独立的系统账户并限制 `data` 目录访问权限。转交便携包时应重新下载或复制一份干净包，不要分享自己使用过的 `data` 文件夹。

提交问题前，请移除 Cookie、token、二维码、手机号、店铺编号、订单、地址、真实商品图片以及截图边缘可能出现的账号信息。安全问题不要公开提交，请按照 [`SECURITY.md`](SECURITY.md) 使用 GitHub 私密漏洞报告。

## 本说明的范围

本说明描述本仓库提供的参考实现。社区分支、修改版和第三方 Adapter 可能采用不同的数据处理方式，使用者应另行检查其文档和源代码。
