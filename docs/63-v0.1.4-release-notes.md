# v0.1.4 Beta 发布说明

本版补充三项面向实际使用的能力：

- 女装和板鞋工作台可以下载业务资料备份，并从备份恢复商品草稿、模板、设置、任务记录和图片副本。
- 支持 `.xlsx`、`.csv`、`.tsv` 整款商品导入，一行对应一个颜色与尺码组合；提供可直接用 Excel/WPS 打开的模板。
- GitHub 标签发布会自动构建 Windows 与 macOS 完整便携包、运行干净环境启动检查、生成 SHA-256 并创建 Release。

板鞋页面合同已经独立建立，但真实商家页面字段仍缺少脱敏证据，所以自动填写保持关闭。不能将女装 T 恤的页面定位器用于板鞋。
