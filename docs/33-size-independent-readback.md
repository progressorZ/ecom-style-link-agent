# 尺码表接入独立综合回读

2026-09-09，继文档32。综合回读的 size 步骤已替换为真实 sizeChart 检查，使用独立 DOM 读取函数，不复用历史填写成功报告。

按照尺寸名称和尺码识别列/行；页面行顺序不影响匹配。预期来自当前 ProductPackage 编译的成衣尺寸，范围仍为肩宽、胸围、厘米单值。额外列、缺失行、合并单元格或不支持的区间结构不能通过。允许的数字文本转换成数值比较，空值不转换成0；保留预期和观察值。

## 真实结果

输入继续使用原测试样本，没有为通过检查修改尺寸：

| 尺码 | 项目 | 输入(cm) | 页面(cm) |
|---|---|---|---|
| S | 肩宽 | 37 | 37.5 |
| M | 肩宽 | 38 | 38.5 |
| S | 胸围 | 86 | 86 |
| M | 胸围 | 90 | 90 |

结果26项matched、1项mismatch、0项unreadable、3项uncovered。差异项为整个sizeChart，其中明确保留两项肩宽差异。尚未覆盖carousel、skuImages、detail；其他完整性阻断仍保留。未保存或发布。

证据：`output/playwright/real-package-readback/dev-size-2026-09-09/input.json` 和 `report.json`。使用Node编译后CLI注入相同回读核心。这个测试证明能发现0.5cm尺寸差异，不证明实际服装尺寸正确；真实资料以测量记录为准。

隔离浏览器测试验证行倒序仍按尺码比较、0.5cm差异被发现、核验不修改DOM、输入与页面一致时该项通过、缺行变成unreadable。下一步接入素材引用与图片独立回读，再补完整覆盖契约和草稿验收。

最新全量验证：110项Node测试＋8项Vitest，共118项通过，TypeScript检查及生产构建通过。
